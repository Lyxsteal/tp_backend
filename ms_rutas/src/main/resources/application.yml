server:
  port: 8085
spring:
  application:
    name: ms_rutas
  datasource:
    url: jdbc:h2:file:/data/rutasdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: santi
    password: 123
    driver-class-name: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
springdoc:
  swagger-ui.path: /swagger-ui.html
